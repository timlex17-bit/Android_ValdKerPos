<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- INTERNET -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- CAMERA FOR BARCODE -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- CAMERA FEATURE -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false"/>

    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>

    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- BLUETOOTH -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30"/>

    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"/>

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30"/>

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ValdKer">

        <!-- ✅ LOGIN -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ NEW: HOME DASHBOARD -->
        <activity
            android:name=".ui.dashboard.HomeDashboardActivity"
            android:exported="false" />

        <!-- ✅ MAIN -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- ✅ CHECKOUT WEBVIEW (HYBRID) -->
        <activity
            android:name=".ui.checkout.CheckoutActivity"
            android:exported="false"
            android:screenOrientation="landscape" />

        <!-- ✅ PRINTER SETTINGS -->
        <activity
            android:name=".print.PrinterSettingsActivity"
            android:exported="false" />

        <activity
            android:name=".ui.ownerchat.OwnerChatActivity"
            android:exported="false" />

        <activity android:name=".ui.productreturns.ProductReturnDetailActivity" />
        <activity android:name=".ui.inventorycount.InventoryCountDetailActivity" />
        <activity android:name=".ui.stockadjustments.StockAdjustmentDetailActivity" />
        <activity android:name=".ui.stockmovements.StockMovementDetailActivity" />
        <activity android:name=".ui.BankAccountActivity" />

    </application>

</manifest>
