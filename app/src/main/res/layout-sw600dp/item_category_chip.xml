<?xml version="1.0" encoding="utf-8"?>
<!--
  Chip kategori - tablet. Sama persis dengan varian ponsel kecuali ukuran:
  area sentuh 52dp dan huruf 14sp, karena bilah kategori tablet hanya
  selebar kolom menu (kolom keranjang memakai sisanya).
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/cardCategory"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginEnd="10dp"
    android:clickable="true"
    android:duplicateParentState="true"
    android:focusable="true"
    android:minWidth="92dp"
    android:minHeight="52dp"
    app:cardBackgroundColor="@color/surface"
    app:cardCornerRadius="@dimen/radius_pill"
    app:cardElevation="0dp"
    app:rippleColor="@color/brand_tint"
    app:strokeColor="@color/stroke"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:duplicateParentState="true"
        android:gravity="center"
        android:minHeight="52dp"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingEnd="16dp">

        <LinearLayout
            android:id="@+id/iconWrapper"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginEnd="10dp"
            android:background="@drawable/bg_category_icon"
            android:backgroundTint="@color/category_icon_bg_tint"
            android:duplicateParentState="true"
            android:gravity="center">

            <ImageView
                android:id="@+id/imgIcon"
                android:layout_width="22dp"
                android:layout_height="22dp"
                android:adjustViewBounds="true"
                android:contentDescription="@null"
                android:scaleType="fitCenter" />
        </LinearLayout>

        <TextView
            android:id="@+id/tvName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:duplicateParentState="true"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/text_primary"
            android:textSize="14sp"
            android:textStyle="bold"
            tools:text="Bakery" />
    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
