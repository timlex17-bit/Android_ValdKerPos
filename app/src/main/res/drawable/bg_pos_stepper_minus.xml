<?xml version="1.0" encoding="utf-8"?>
<!-- Tombol kurang: putih di atas pil abu. -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/pos_accent_tint_pressed">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFFFF" />
            <corners android:radius="@dimen/pos_radius_pill" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/pos_card" />
            <corners android:radius="@dimen/pos_radius_pill" />
        </shape>
    </item>
</ripple>
